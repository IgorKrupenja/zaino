<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <!-- prefetch category images -->
    <%= htmlWebpackPlugin.options.prefetchLinks %>
    <title>Zaino</title>
  </head>
  <body>
    <div id="app">
      <!-- Page loading indicator, shown before bundle is downloaded -->
      <!-- or before React mounts the app after login -->
      <div class="loader-container loader-container--full-height">
        <div class="loader loader--center" />
      </div>
    </div>
  </body>
</html>
